void define2DArray(int *array, size_t deptCount, size_t empCount) {
    int row, column;
    for (row = 0; row < empCount; row++) {

        for (column = 0; column < deptCount; column++) {
            
        }
    }
}
